<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Zaposleni</title>
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
    }

    h1 {
      text-align: center;
      margin-bottom: 20px;
    }

    ul {
      list-style-type: none;
      padding: 0;
    }

    li {
      margin: 10px 0;
    }

    a {
      text-decoration: none;
      color: #007bff;
      font-size: 18px;
      position: relative;
      padding-right: 20px;
      transition: color 0.3s;
    }

    a::after {
      content: '';
      position: absolute;
      right: 0;
      top: 50%;
      transform: translateY(-50%);
      width: 0;
      height: 0;
      border-top: 5px solid transparent;
      border-bottom: 5px solid transparent;
      border-left: 5px solid #007bff;
      transition: width 0.3s;
    }

    a:hover {
      color: #0056b3;
    }

    a:hover::after {
      width: 10px;
    }

    .journals-button {
      text-align: center;
      margin-top: 20px;
    }

    .btn {
      display: inline-block;
      padding: 10px 20px;
      color: #fff;
      background-color: #007bff;
      text-decoration: none;
      border-radius: 25px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      transition: background-color 0.3s, box-shadow 0.3s;
    }

    .btn:hover {
      background-color: #0056b3;
      color: #fff;
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
    }
  </style>
</head>
<body>
  <div>
    <h1>Seznam zaposlenih</h1>
    <ul>
      <% zaposleni.forEach(zaposlen => { %>
        <li><a href="/zaposleni/<%= zaposlen.id_zaposleni %>"><%= zaposlen.ime %> <%= zaposlen.priimek %></a></li>
      <% }) %>
    </ul>
    <div class="journals-button">
      <a href="/journals" class="btn">Dnevniki</a>
    </div>
  </div>
</body>
</html>
